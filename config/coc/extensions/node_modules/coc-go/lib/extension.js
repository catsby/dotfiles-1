"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const coc_nvim_1 = require("coc.nvim");
const tools_1 = require("./utils/tools");
const commands_1 = require("./commands");
function activate(context) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        const config = coc_nvim_1.workspace.getConfiguration().get('go', {});
        if (config.enable === false) {
            return;
        }
        const command = config.commandPath || (yield tools_1.goToolBin('gopls'));
        if (!(yield tools_1.commandExists(command))) {
            yield tools_1.installGoTool('gopls');
        }
        const serverOptions = { command };
        const clientOptions = {
            documentSelector: ['go']
        };
        const client = new coc_nvim_1.LanguageClient('go', 'gopls', serverOptions, clientOptions);
        context.subscriptions.push(coc_nvim_1.services.registLanguageClient(client), coc_nvim_1.commands.registerCommand("go.version", () => commands_1.version()), coc_nvim_1.commands.registerCommand("go.install.gopls", () => commands_1.installGopls(client)));
    });
}
exports.activate = activate;
//# sourceMappingURL=extension.js.map