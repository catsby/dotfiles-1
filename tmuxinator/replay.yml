# ~/.tmuxinator/replay.yml

name: replay
root: /Users/clint/Projects/Go/src/github.com/catsby 

windows:
  - heroprotocol:
      root: /Users/clint/Projects/Go/src/github.com/Blizzard/heroprotocol
      layout: c6ee,316x71,0,0[316x44,0,0,0,316x26,0,45,1]
      # Synchronize all panes of this window, can be enabled before or after the pane commands run.
      # 'before' represents legacy functionality and will be deprecated in a future release, in favour of 'after'
      # synchronize: after
      panes:
        - 
        - envchain cts-aws-second $SHELL
  - replaycode:
      root: /Users/clint/Projects/Go/src/github.com/catsby/replaything
      layout: c6ee,316x71,0,0[316x44,0,0,0,316x26,0,45,1]
      # Synchronize all panes of this window, can be enabled before or after the pane commands run.
      # 'before' represents legacy functionality and will be deprecated in a future release, in favour of 'after'
      # synchronize: after
      panes:
        - 
        - envchain cts-aws-second $SHELL

  - serverless-proto:
      root: /Users/clint/Projects/Go/src/github.com/catsby/serverless-proto
      layout: c6ee,316x71,0,0[316x44,0,0,0,316x26,0,45,1]
      # Synchronize all panes of this window, can be enabled before or after the pane commands run.
      # 'before' represents legacy functionality and will be deprecated in a future release, in favour of 'after'
      # synchronize: after
      panes:
        - 
        - envchain cts-aws-second $SHELL

